#include "main.h"

/**
 * _abs - Calcula el valor absoluto de un entero.
 *
 * @n: El entero cuyo valor absoluto se va a calcular.
 *
 * Return: El valor absoluto del entero n.
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	return (0);
}
