#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/* General Utility; prints a single character to standard output */
int _putchar(char c);

/* 2-mul.c, 3-mul.c, 4-add.c; converts a string to an integer */
int _atoi(char *s);

/* 4-add.c (and others); checks if a string contains only digits */
int _is_digit(char *s);

/* Utility; prints a string followed by a new line */
void _puts(char *str);

/*100-change.c prints the minimum number of coins
 * to make change for an amount of money*/



#endif
